"""Vault Entity Method Implementations.

Transfer.execute() and Balance.refresh() are now natively implemented
in ggg.finance using ic_basilisk_toolkit.wallet.Wallet directly.
The vault extension is a UI-only wrapper.

No entity method overrides are needed.
"""
